import MediaRow from './MediaRow'

export default function BookmarkRow(
  props
) {
  return (
    <MediaRow
      title="Bookmarked"
      {...props}
    />
  )
}

export default BookmarkRow